module Main (main) where

import qualified Monomorphic
import qualified Polymorphic
import qualified Polymorphic.Terms
import qualified Polymorphic.Types
import qualified Polymorphic.Simple.Inference

main :: IO ()
main = print "Hello World"
